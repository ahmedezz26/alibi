{
  "mcpServers": {
    "alibi": {
      "command": "uvx",
      "args": ["--from", "agent-alibi", "alibi-mcp"],
      "env": {
        "TYPESAFE_API_KEY": "${TYPESAFE_API_KEY}",
        "ALIBI_JUDGE_BACKEND": "typesafe",
        "ALIBI_ALLOW_PAID_MODELS": "1"
      }
    }
  }
}
